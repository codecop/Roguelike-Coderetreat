micronaut:
  application:
    name: room-sample
  server:
    port: 8003
  router:
    static-resources: 
        default:
            enabled: false

  http:
    services:
      foo:
        url: 'http://127.0.0.1:8080'

netty:
  default:
    allocator:
      max-order: 3
